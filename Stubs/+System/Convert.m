classdef Convert
    
    methods (Static)
        
        function d = ToDouble(object)
            % Don't need to do anything. Just need to exist for now.
            d = object;
        end
        
    end
    
end

